import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    photo: {
        width: 50,
        height: 50,
        overflow:'hidden',
        borderRadius: 150/2,
        shadowColor: 'black',
    },
});

export default styles;